import React from "react";
import ReactDOM from "react-dom/client";
import SongDayScreener from "./SongDayScreener.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SongDayScreener />
  </React.StrictMode>
);
